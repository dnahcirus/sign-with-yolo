FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json* yarn.lock* ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["sh", "-c", "echo 'Frontend running at http://localhost:3000' && npm run dev -- --host 0.0.0.0"]
